<resources>
    <!-- Define your custom primary color -->
    <color name="custom_primary_color">#FF5722</color>

    <!-- Define your theme with the custom primary color -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize other attributes as needed -->
        <item name="colorPrimary">@color/custom_primary_color</item>
<!--        <item name="colorPrimaryDark">@color/custom_primary_color_dark</item>-->
<!--        <item name="colorAccent">@color/custom_accent_color</item>-->
        <!-- Add more customizations if necessary -->
    </style>

    <style name="MainActivityTheme.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@android:style/Theme.DeviceDefault</item>
    </style>
</resources>